DATABASE_URL="postgresql://postgres:replace-me@db:5432/remembrall?schema=public"
PORT=8989
HOST=0.0.0.0
POSTGRES_PASSWORD="replace-me"
POOLSIDE_API_KEY=
POOLSIDE_BASE_URL="https://inference.poolside.ai"
POOLSIDE_MODEL="poolside/laguna-xs.2"
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
# Cloudflare Tunnel — paste the connector token from
# Zero Trust → Networks → Tunnels → <your tunnel> → Install connector.
# Only required when running `docker compose -f docker-compose.yml up` on the server.
CLOUDFLARE_TUNNEL_TOKEN=
# Optional: override the Docker image tag pulled from GHCR.
# Default is 'latest'.
IMAGE_TAG=
